<div>
    <span>Player 1: </span>
    <div>
        {{#each player1}}
        <option>{{this.first_name}} {{this.last_name}}</option>
        {{/each}}
    </div>
    <span>Player 2: </span>
    <div>
        {{#each player2}}
        <option>{{this.first_name}} {{this.last_name}}</option>
        {{/each}}
    </div>
</div>